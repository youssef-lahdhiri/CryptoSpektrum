'use client'
import { useContext } from "react";
// import { RightContext } from "./right";

export default function Main() {
  // const value = useContext(RightContext);
  // console.log(`value is ${value}`)
  return (
    <div className="bg-blue-200 absolute top-1/2 right-1/2  translate-x-1/2"> this is converted value {value}</div>
  );
}
